version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.1.2
    working_directory: ~/project/
    steps:
      - checkout
      - persist_to_workspace:
          root: ~/project/
          paths:
            - ./
  uxpin-code-cli:
    docker:
      - image: circleci/node:8.1.2
    working_directory: ~/project/packages/uxpin-code-cli
    steps:
      - attach_workspace:
          at: ~/project
      # Yarn install with cache
      - restore_cache:
          key: uxpin-code-cli-dependencies-cache-{{ checksum "package.json" }}
      - run:
          name: install-dependencies
          command: yarn install
      - save_cache:
          key: uxpin-code-cli-dependencies-cache-{{ checksum "package.json" }}
          paths:
            - ./node_modules
      # Download test repositories with cache
      - restore_cache:
          key: uxpin-code-cli-test-repos-{{ checksum "shell_scripts/test_resources/Makefile" }}
      - run:
          name: allow-cloning-git-dependencies
          command: mkdir ~/.ssh/ && echo -e "Host github.com\n\tStrictHostKeyChecking no\n" > ~/.ssh/config
      - run:
          name: test-resources
          command: make test-resources
      - save_cache:
          key: uxpin-code-cli-test-repos-{{ checksum "shell_scripts/test_resources/Makefile" }}
          paths:
            - ./test/resources/repos
      # Install Google Chrome with cache
      - run:
          name: Create file with month number to force Chrome version refresh every month
          command: date "+%m/%Y" > month.txt
#      - restore_cache:
#          key: google-chrome-{{ checksum "month.txt" }}
      - run:
          name: Install latest version of Google Chrome
          command: |
            wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
            sudo dpkg -i google-chrome-stable_current_amd64.deb; sudo apt-get -fy install
#      - save_cache:
#          key: google-chrome-{{ checksum "month.txt" }}
#          paths:
#            - ./google-chrome-stable_current_amd64.deb
      # Build and test
      - run:
          name: build
          command: make build
      - run:
          name: check
          command: make check
      - run:
          name: test
          command: make test-ci
          environment:
            CHROME_PATH: /usr/bin/google-chrome
      - store_test_results:
          path: ./reports
      - run:
          name: code-coverage
          command: ./packages/uxpin-code-cli/node_modules/.bin/codecov -F unittests -F uxpin_code_cli
          working_directory: ~/project/

workflows:
  version: 2
  build_and_test_packages:
    jobs:
      - build
      - uxpin-code-cli:
          requires:
            - build
